import 'package:flutter/material.dart';
import '../../../../core/widgets/placeholder_screen.dart';

/// TODO: ReportsManagementCubit — بلاغات pending/reviewed + تعطيل العقار المبلَّغ عنه
class ReportsManagementScreen extends StatelessWidget {
  const ReportsManagementScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const PlaceholderScreen(title: 'إدارة البلاغات');
  }
}
