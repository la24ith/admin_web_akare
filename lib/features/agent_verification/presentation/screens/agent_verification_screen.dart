import 'package:flutter/material.dart';
import '../../../../core/widgets/placeholder_screen.dart';

/// TODO: AgentVerificationCubit — الوكلاء is_verified_agent=false + زر توثيق
class AgentVerificationScreen extends StatelessWidget {
  const AgentVerificationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const PlaceholderScreen(title: 'توثيق الوكلاء');
  }
}
